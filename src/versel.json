{
    "version": 2,
    "builds": [
        {
            "src": "./appwrite.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/appwrite(.*)",
            "dest": "/appwrite.js"
        }
    ]
}
